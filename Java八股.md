# Java八股



## javase





### 1.接口与抽象类的区别

**1. 接口：一份“必须完成的任务清单”**

假设你开了一家奶茶店，想招兼职店员。这时候你会列一份“店员任务清单”：必须会做奶茶、必须会收银、必须会擦桌子。这份清单就是**接口**——它不教你具体怎么做（比如奶茶加多少糖），但规定了“必须会做什么”。

- •

	**特点**：

	- •

		清单里的任务（方法）是“死规定”，必须全部完成（实现），否则不能上岗（不能实例化）。

	- •

		一个店员（类）可以同时打多份工（实现多个接口），比如既当奶茶店员，又当咖啡店员（只要另一份清单的任务也能完成）。

	- •

		清单本身没有“工具”（成员变量），比如不会给你发围裙（变量），但要求你必须穿围裙（方法）。

**举个代码例子**：

我之前做的二手平台有个`CacheService`接口，相当于“缓存任务清单”：

```java
public interface CacheService {
    void set(String key, Object value); // 必须会存数据
    Object get(String key); // 必须会取数据
}
```

不管用Redis还是Memcached当缓存，都得按这个清单实现这两个方法——这就是接口的“约束”作用。

2. 抽象类：一份“半成品模板

还是说奶茶店，你发现新员工上手太慢，于是做了一份“奶茶制作模板”：泡茶步骤（烧水→放茶叶→焖3分钟）、加奶步骤（热牛奶→倒杯里）都写好了，但糖放多少、要不要加小料（珍珠/椰果）没规定。这份模板就是**抽象类**——它提供了基础步骤（代码），但留了“自定义空间”（抽象方法）。

- •

	**特点**：

	- •

		模板里有现成的工具（成员变量），比如已经买了烧水壶（变量），新员工直接用就行。

	- •

		模板里的步骤（方法）分两种：

		- •

			已经写好的（具体方法）：比如“烧水到100度”，新员工直接照做。

		- •

			留空的任务（抽象方法）：比如“加多少糖”，新员工必须在自己的“子类”（比如“标准奶茶”“少糖奶茶”）里补上。

	- •

		模板只能“单传”（单继承）：比如“水果茶”模板只能基于“奶茶模板”改，不能同时基于“咖啡模板”改（因为Java类只能继承一个抽象类）。

**举个代码例子**：

我之前写的`BaseController`抽象类，相当于“接口响应模板”：

```java
public abstract class BaseController {
    // 现成的工具：日志记录器（成员变量）
    protected Logger logger = LoggerFactory.getLogger(getClass()); 

    // 已经写好的方法：统一返回成功响应（具体方法）
    protected Result success(Object data) {
        return new Result(200, "成功", data);
    }

    // 留空的任务：获取用户信息（抽象方法），子类必须自己实现
    protected abstract User getCurrentUser();
}
```

所有具体的Controller（比如`UserController`）继承它后，直接用`success()`方法，再补上`getCurrentUser()`的具体逻辑就行——这就是抽象类的“复用+扩展”作用。





- **接口**：是“任务清单”，管“必须做什么”，不提供工具（成员变量），支持多实现（打多份工）。
- **抽象类**：是“半成品模板”，管“怎么做一部分”，提供工具（成员变量），只能单继承（只能基于一个模板改）。













## 集合



### 2.（1）hashmap数据结构是什么?       （2） 为什么hashmap在jdk1.8引入红黑树?      （3）hashmap扩容机制?



（1） 数组+链表+红黑树

(2)	JDK1.7的问题：链表太长，查得慢, 红黑树把“排队”变成“分层找”

JDK1.8的改进是：当链表长度超过**8**（默认阈值），且数组长度≥64时，链表会自动转换成红黑树。这样一来，即使很多键冲突到同一个桶，查询效率也不会暴跌。

(3)扩容的触发条件是：**当前元素数量 > 负载因子 × 数组容量**（阈值 = 负载因子 × 容量）。

- **负载因子（Load Factor）**：默认是0.75（JDK设计时权衡的结果，空间和时间的平衡）。
- **数组容量**：HashMap底层数组的大小，必须是2的幂次方（比如16、32、64…）。

扩容过程：  创建新数组，重新分配元素，替换旧数组

本质是空间换时间













### 3.（1）为什么数组长度为2的幂次方?     （2）  为什么jdk7到jdk8要把头插改尾插?   （3）   为什么解决hash冲突的方式为链表+红黑树?

（1）

- 2的幂次方可以用位运算（`hash & (newCap - 1)`）快速计算索引，比取模（`hash % newCap`）更高效。
- 2倍扩容能保证哈希值的低位变化，减少重新分配后元素的冲突概率（比如原索引是 `hash % 16`，新索引要么是原位置，要么是原位置+16，分布更均匀）。

（2）JDK7的头插法在多线程扩容时会**导致链表成环**（死循环），而JDK8改用尾插法，**保持了链表的原有顺序**，避免了这种问题。虽然尾插法不能完全解决多线程下的所有问题（比如数据丢失），但至少修复了最致命的死循环bug，提升了并发场景下的稳定性。

(3)**链表+红黑树的本质是“分层优化”**

HashMap的冲突解决策略，本质上是**“用简单结构处理常见场景，用复杂结构兜底极端场景”**











### 4.ArrayList与LinkedList区别?

底层数据结构：**ArrayList**：基于**动态数组**实现。**LinkedList**：基于**双向链表**实现。

- **ArrayList**：像快递柜，**查得快，插删慢（中间），省空间**。
- **LinkedList**：像链式货架，**插删快（已知位置），查得慢，费空间**。

实际开发中，**ArrayList是更常用的选择**（因为大部分场景是查询为主），只有在需要频繁中间插入/删除时（比如实现队列、栈），才考虑LinkedList。















### 5.(1)cocurrentHashMap如何实现?     (2)从jdk7到jdk8做了什么升级?

 (1)

- **JDK7**：用“分段锁”将数据分块，降低锁竞争，但锁粒度粗、扩容慢。
- **JDK8**：用“CAS + synchronized 锁桶头”+“红黑树”，锁粒度更细、支持多线程扩容，性能更优。

简单说：**JDK7 是“分楼层锁门”，JDK8 是“每家每户独立锁”+“智能货架（红黑树）”** 😊 这样设计让 ConcurrentHashMap 在多线程环境下更高效、更灵活。



(2)JDK7 的分段锁设计在多线程高并发场景下暴露了明显问题：

- **锁竞争激烈**：如果多个线程频繁操作同一个 Segment（比如哈希冲突严重），锁会成为瓶颈，并发度下降。
- **扩容效率低**：Segment 内部的数组扩容时需要加锁，无法利用多线程并行迁移数据。
- **空间浪费**：即使数据量很小，也需要初始化多个 Segment（默认 16 个），占用额外内存。

JDK8 通过更细粒度的锁（桶级别）、CAS 原子操作、红黑树优化和多线程扩容，彻底解决了这些问题，使 ConcurrentHashMap 在高并发场景下的性能大幅提升。











## jvm







### 6.jvm如何调优?有哪些垃圾回收算法?

垃圾回收（GC）的本质是**自动回收不再使用的内存**，避免内存泄漏。

JVM 调优的核心目标是：**降低 GC 停顿时间、减少 Full GC 频率、提升应用吞吐量**。

- **垃圾回收算法**：标记-清除（简单）、复制（高效）、标记-整理（无碎片）、分代收集（主流）；
- **JVM 调优**：明确目标→选收集器→调堆参数→优化对象分配→监控验证；
- **核心原则**：根据应用类型（低延迟/高吞吐）选择策略，结合数据持续迭代。

实际调优中，“没有最优配置，只有最适合的配置”～ 😊











### 7.JVM内存空间?

JVM 内存空间的设计围绕 **“线程隔离”** 和 **“全局共享”** 展开：

- 线程私有区域（计数器、栈）保障线程独立执行；
- 线程共享区域（堆、方法区）实现数据全局可见；
- 合理调优各区域参数（如堆大小、栈大小），可显著提升应用性能和稳定性。

理解 JVM 内存空间是定位 OOM、GC 问题的基础，也是后端开发的核心技能之一～ 😊











### 8.逃逸分析?

逃逸分析是 JVM 优化内存分配和代码执行的关键技术，通过判断对象是否逃逸，JVM 可以实现栈上分配、标量替换、锁消除等优化，从而减少 GC 压力、提升性能。理解逃逸分析有助于开发者在编写代码时更有意识地控制对象作用域，与 JVM 优化机制协同工作。

简单说：**逃逸分析是 JVM 的“对象作用域侦探”，帮程序“少花钱（堆内存）多办事（高性能）”** 😊













### 9.如何避免内存溢出异常?

内存溢出（OutOfMemoryError, OOM）是 Java 应用中最常见的异常之一，通常由内存分配不足或内存泄漏导致。避免 OOM 需要从 **内存分配优化**、**代码逻辑修复**、**JVM 参数调优** 三个层面入手。

避免内存溢出的关键是 **“预防为主，监控为辅”**：

- **预防**：优化对象生命周期、减少大对象分配、修复内存泄漏；
- **监控**：通过日志和工具实时跟踪内存使用；
- **调优**：根据场景调整 JVM 参数（堆、元空间、栈大小）和垃圾收集器。

记住：**没有“一劳永逸”的解决方案，需要结合具体业务场景持续迭代优化** 😊













## JUC









### 10.什么是乐观锁?CAS实现原理?什么是悲观锁?synchronized和reentrantLock的区别?

- **乐观锁 vs 悲观锁**：乐观锁“先修改后检查”（无锁，适合读多写少），悲观锁“先加锁后修改”（有锁，适合写多冲突高）。
- **CAS**：通过 CPU 指令级的原子操作实现无锁乐观锁，需注意 ABA 问题（用版本号解决）。
- **synchronized vs ReentrantLock**：`synchronized`简单易用（JVM 优化后性能接近），`ReentrantLock`功能更灵活（可中断、超时、公平锁）。

**选择建议**：

- 读多写少、冲突低：优先乐观锁（CAS）；
- 写多冲突高：优先 `synchronized`（简单）或 `ReentrantLock`（需要高级功能）；
- 需要细粒度控制（如条件变量）：选 `ReentrantLock`。







### 11.synchronized锁升级流程?是否为可重入锁，可重入锁为了保证什么?

- **synchronized 锁升级流程**：根据竞争激烈程度，从偏向锁（无竞争）→ 轻量级锁（低竞争）→ 重量级锁（高竞争）动态调整，优化性能。
- **synchronized 是可重入锁**：通过 Monitor 计数器实现，允许同一线程多次获取同一锁，避免嵌套同步块的死锁。
- **可重入锁的作用**：支持复杂调用链中的嵌套同步，保证代码正确性和线程安全。









### 12.原子类如何实现

原子类的实现核心是 **CAS 无锁算法**，通过循环重试保证原子性。具体步骤为：

1. 读取变量的当前值；
2. 尝试用 CAS 更新为新值（仅当当前值等于预期旧值时成功）；
3. 若失败则重试，直到成功。

原子类通过 `Unsafe`类调用底层 CPU 指令，避免了锁的开销，在低竞争场景下性能更优。同时，通过版本号机制（如 `AtomicStampedReference`）解决 ABA 问题，保证复杂场景下的正确性。











### 13.volatile关键字的作用?

volatile`的作用可以简单记为两点：

1. **让共享变量“看得见”**：一个线程修改后，其他线程立即看到最新值（解决可见性问题）。
2. **禁止“打乱顺序”**：保证代码按写的顺序执行（解决指令重排序问题）。

**什么时候用 `volatile`？**

当需要多线程共享一个变量，且需要“及时看到修改”或“禁止乱序”时（如状态标志位、双重检查锁定）。

**什么时候不用 `volatile`？**

当需要对变量的修改加锁（保证原子性）时（如 `i++`、转账操作），应该用 `synchronized`或原子类（如 `AtomicInteger`）。











### 14.JMM内存模型?指令重排序?happens before原则?

- JMM 是“多线程的内存交通规则”，管着线程如何“看”和“改”共享内存。
- 指令重排序是“优化小把戏”，但可能让多线程“看错”步骤顺序。
- happens-before 是“因果律”，规定“哪些操作必须按顺序发生”，确保多线程程序不乱套。











### 15.AQS队列如何实现?如何实现公平锁?

- **AQS 队列**：通过双向链表管理等待线程，CAS 保证队列操作的原子性，`LockSupport`控制线程阻塞/唤醒。
- **公平锁**：在 `tryAcquire()`中通过 `hasQueuedPredecessors()`检查队列是否有等待线程，强制新线程排队，避免插队。

**一句话记忆**：AQS 是“线程的排队管家”，公平锁是“按顺序叫号的银行”，非公平锁是“允许插队的 VIP 银行”。











### 16.线程池的核心参数?提交任务流程?核心线程池如何计算为什么?

- **核心参数**：`corePoolSize`（固定线程数）、`maximumPoolSize`（最大线程数）、`keepAliveTime`（空闲线程存活时间）、`workQueue`（任务队列）、`handler`（拒绝策略）。
- **提交流程**：核心线程 → 队列 → 临时线程 → 拒绝策略。
- **线程数计算**：根据任务类型（CPU/IO 密集型）和 CPU 核心数调整，目标是平衡资源利用率和响应速度。

**一句话记忆**：线程池是“任务处理的工厂”，核心参数决定“工厂规模”，提交流程是“任务从排队到执行的路径”，线程数计算是“根据任务类型定产能”。









## Spring

### 17.springboot用了哪些设计模式?

Spring Boot 的设计模式围绕 **“解耦”“复用”“灵活”** 展开：

- **工厂模式**：统一管理 Bean 的创建；
- **单例模式**：减少资源浪费；
- **依赖注入**：降低组件间耦合；
- **模板方法**：封装通用逻辑，减少重复代码；
- **策略模式**：灵活切换功能实现；
- **适配器模式**：兼容不同接口；
- **观察者模式**：实现事件驱动的通知。

这些模式共同构建了 Spring Boot“简洁、高效、易扩展”的核心特性，理解它们能帮助你更高效地使用和定制 Spring Boot 应用～ 😊













### 18.IOC?



IoC 是“程序设计的权力交接”——将对象的创建和管理交给 Spring 容器，应用程序只需关注业务逻辑。通过依赖注入（DI），Spring 容器自动为对象注入所需的依赖，实现解耦、复用和高效管理。理解 IoC 是掌握 Spring 框架的核心基础～ 😊











### 19.讲讲循环依赖在spring如何解决的?



Spring 解决循环依赖的核心是 **三级缓存机制**：

1. **一级缓存**：存储完全初始化的 Bean，对外提供最终实例；
2. **二级缓存**：存储提前暴露的未完全初始化的 Bean，解决循环依赖；
3. **三级缓存**：存储 Bean 的工厂对象，用于生成提前暴露的实例。

**关键条件**：单例作用域、字段/Setter 注入。

**无法解决**：构造器注入、原型作用域、长链循环依赖。

理解这一机制后，你可以更从容地处理 Spring 中的循环依赖问题，或在遇到无法解决的场景时快速定位原因～ 😊











### 20.AOP实现原理?有哪些代理模式?运行代理还是编译代理?

- **AOP 核心**：通过代理模式分离横切关注点与业务逻辑。
- **代理模式**：
	- 静态代理：编译时生成，手动编写，适合简单场景；
	- 动态代理：运行时生成，反射实现，灵活适配多目标类（JDK 代理基于接口，CGLIB 基于继承）。
- **Spring AOP**：默认使用动态代理（JDK/CGLIB），通过 `BeanPostProcessor`生成代理对象，在方法前后插入切面逻辑。

理解这些后，你可以更清晰地掌握 AOP 的底层机制，以及如何在实际项目中选择合适的代理方式～ 😊











### 21.spring的事务什么情况会失效?

1. **避免吞异常**：确保 `RuntimeException`/`Error`被抛出（或显式调用 `TransactionAspectSupport.currentTransactionStatus().setRollbackOnly()`）。
2. **统一事务边界**：事务方法应被事务方法调用（避免非事务方法调用事务方法）。
3. **检查数据库引擎**：使用 InnoDB（MySQL）或其他支持事务的引擎。
4. **避免自调用**：通过代理对象调用事务方法（如将方法拆分到不同类）。
5. **正确配置传播行为**：根据业务需求选择 `REQUIRED`/`REQUIRES_NEW`等传播行为。
6. **显式指定 `rollbackFor`**：对受检异常添加 `@Transactional(rollbackFor = ...)`。
7. **确保事务管理器配置**：在 Spring 容器中注册 `PlatformTransactionManager`。
8. **使用 Spring 管理的连接**：通过 `DataSourceUtils.getConnection(dataSource)`获取连接。

通过以上措施，可以有效避免 Spring 事务失效，确保数据一致性～ 😊







## redis



### 22.Redis基本数据类型?常见应用场景

|        **场景**        | **推荐数据类型** |                 **示例**                  |
| :--------------------: | :--------------: | :---------------------------------------: |
|      缓存热点数据      | 字符串（String） |          缓存商品详情、用户信息           |
| 计数器（点赞、阅读量） | 字符串（String） |         `INCR article:1001:views`         |
|        消息队列        |   列表（List）   |   生产者 `RPUSH`消息，消费者 `LPOP`消费   |
| 存储对象（用户、商品） |   哈希（Hash）   |    `HSET user:1001 name "张三" age 20`    |
|     去重、共同好友     |   集合（Set）    |    `SADD user:1001:friends 1002 1003`     |
|  排行榜（销量、积分）  | 有序集合（ZSet） | `ZADD product:rank 100 200`（分数为销量） |
|   统计活跃用户（UV）   |  位图（Bitmap）  |   `SETBIT user:active:20240810 1001 1`    |
|  附近的人（位置服务）  | 地理空间（Geo）  | `GEORADIUS user:location 120.1 30.2 5 km` |











### 23.缓存穿透?雪崩?击穿?



- 穿透：查不存在的，缓存和数据库都没有；
- 雪崩：大量缓存同时失效，数据库被挤爆；
- 击穿：单个热点缓存失效，大量请求涌向数据库。









### 24.缓存数据一致性?

缓存数据一致性的核心是 **平衡性能与一致性需求**：

- 强一致：适合高敏感场景（如金融），但性能开销大；
- 最终一致：适合高并发场景（如电商），通过异步或 TTL （缓存过期时间）实现；
- 弱一致：适合对一致性要求低的场景（如统计），通过 TTL 或定时任务解决。

实际项目中，需结合业务场景（如数据变更频率、用户对一致性的容忍度）选择合适的方案，并通过监控（如缓存命中率、数据库与缓存数据差异）持续优化。











### 25.Redis为什么快?解释I/O多路复用模型?

Redis 快的核心是 **“内存存储 + 高效数据结构 + I/O 多路复用”**：

1. **内存存储**：数据存在内存中，读写速度远快于硬盘；
2. **高效数据结构**：针对不同场景设计最优数据结构（如哈希、跳表），减少计算时间；
3. **I/O 多路复用**：用一个线程监控多个客户端事件，避免线程阻塞，提升并发能力。

### **一句话总结**

Redis 快是因为它“把数据存在内存里，用最聪明的方式操作数据，还能同时照顾很多客户端”，就像一个“超级高效的餐厅服务员”～ 😊













### 26.Redis实现登录机制?

用 Redis 实现登录机制的核心是：

1. **生成唯一令牌**（如 UUID）；
2. **存储令牌与用户关联**（Redis 的 `String`类型，设置过期时间）；
3. **验证令牌有效性**（后续请求携带令牌，从 Redis 查询是否存在）。

Redis 凭借其高速读写和自动过期特性，完美解决了会话管理的高效性和临时性问题。新手只需掌握“生成-存储-验证”三步，就能快速实现登录机制～ 😊













### 27.Redis实现防抖和节流?



- **防抖**：用 Redis 存储“上一次触发时间”，超过时间窗口才执行；
- **节流**：用 Redis 存储“上一次执行时间”或“计数器”，限制固定时间内的执行次数；
- **Redis 优势**：原子性、高性能、自动清理，完美适配高并发场景。

通过以上方法，新手可以快速用 Redis 实现防抖和节流，优化应用的性能和用户体验～ 😊















### 28.主从同步?持久化机制?

- **主从同步**：通过主节点（写）和从节点（读）的配合，实现数据冗余和读写分离，保障高可用；
- **持久化机制**：
	- RDB（快照）：适合定期备份，恢复快但可能丢数据；
	- AOF（日志）：适合数据完整性要求高的场景，恢复慢但数据更全；
	- 混合持久化：结合两者优点，生产环境首选。

理解主从同步和持久化机制，能帮助你更好地规划 Redis 的部署（如主从架构、持久化配置），避免数据丢失和高并发下的性能问题～















### 29.bigkey和hotkey如何优化?

- **BigKey 优化核心**：拆分、压缩、换结构、限大小；
- **HotKey 优化核心**：本地缓存、限流、分散热点、预加载、多级缓存；
- **关键原则**：预防胜于治疗（提前设计数据结构和访问模式），监控先行（及时发现并处理）。

通过以上方法，新手可以有效解决 Redis 中 BigKey 和 HotKey 带来的性能问题，提升系统的稳定性和吞吐量～ 😊











## mysql

### 30.Mysql事务隔离级别?ACID?

- **ACID**：事务的四大特性（原子、一致、隔离、持久），是数据库可靠的基石。
- **隔离级别**：控制事务并发干扰的程度，MySQL 默认“可重复读”（平衡一致性与性能）。
- **实现方式**：锁机制（强制互斥）和 MVCC（无锁读，高效并发）。

理解这些后，你能明白为什么转账时不会出现“钱凭空消失”，也能根据业务需求选择合适的事务隔离级别～ 😊











### 31.MVCC下，可重复读如何实现的?还有幻读出现吗?

- **MVCC 实现可重复读**：通过隐藏字段（`DB_TRX_ID`、`DB_ROLL_PTR`）和 Undo 日志保存数据历史版本，事务启动时生成 Read View，后续读操作基于该视图筛选版本，确保结果一致。
- **幻读的解决**：MySQL InnoDB 通过间隙锁锁定索引区间，防止其他事务插入新数据，彻底避免幻读。

简单来说，MVCC 让事务“看到的是启动时的数据快照”，而间隙锁让“快照不会被其他事务的新数据污染”，两者共同保证了 RR 隔离级别的可靠性～ 😊











### 32.间隙锁?临键锁?回表查询如何避免?

- **间隙锁**：锁定索引间隙，防止其他事务插入新数据，避免幻读；
- **临键锁**：行锁 + 间隙锁的组合，锁定当前行和前面的间隙，更严格的隔离；
- **回表查询**：通过非主键索引查询时需回表获取完整数据，可通过覆盖索引、优化索引顺序避免。

理解这些概念后，你能更好地优化 MySQL 的并发性能和查询效率～ 😊











### 33.常见索引?索引失效?

- **常见索引**：主键（唯一、聚簇）、唯一（唯一、非聚簇）、普通（加速查询）、全文（文本搜索）、组合（多条件加速）。
- **索引失效**：违反最左匹配、类型不匹配、函数操作、隐式转换、覆盖不全、重复率过高。
- **核心原则**：索引不是万能的，需根据业务场景合理设计，避免滥用。

通过理解这些，你能更高效地使用索引，提升查询性能～ 😊











### 34.explain关键字?



#### **一、`EXPLAIN`关键字：MySQL 的“查询透视镜”**

`EXPLAIN`是 MySQL 中用于 **分析 SQL 查询执行计划** 的核心工具。它不会实际执行查询，而是“模拟”MySQL 处理查询的过程，输出详细的执行步骤和优化信息，帮助你理解：

- 查询是如何被解析和执行的？
- 是否使用了索引？
- 扫描了多少行数据？
- 哪些步骤可能成为性能瓶颈？

#### **二、`EXPLAIN`的基本用法**

在 SQL 语句前加上 `EXPLAIN`（或 `EXPLAIN FORMAT=JSON`输出更详细信息），即可查看执行计划。

**示例**：

```mysql
-- 普通查询
EXPLAIN SELECT * FROM user WHERE age > 20;

-- 带 JOIN 的查询
EXPLAIN SELECT u.name, o.order_time 
FROM user u 
JOIN order o ON u.id = o.user_id 
WHERE u.age > 20 AND o.order_time > '2024-01-01';
```





#### 总结

- **`EXPLAIN`的作用**：分析查询执行计划，定位性能瓶颈；
- **核心字段**：`type`（访问类型）、`key`（实际索引）、`rows`（扫描行数）、`Extra`（额外提示）；
- **优化方向**：确保 `type`不是 `ALL`，`key`非 `NULL`，`rows`尽可能小，`Extra`无严重性能提示；
- **进阶工具**：`EXPLAIN ANALYZE`（MySQL 8.0+）用于实际执行统计。











### 35.索引数据结构?为何使用B+树?







#### 总结

- **索引数据结构**：哈希表（O(1) 但无序）、平衡树（O(logn) 但树高）、B 树（多叉降低树高）、B+ 树（优化范围查询和磁盘 I/O）。
- **B+ 树的优势**：
	- 范围查询高效（叶子节点链表）；
	- 磁盘 I/O 次数少（节点大小匹配磁盘块）；
	- 结构稳定，支持高并发和事务。

数据库选择 B+ 树作为索引，本质是为了在“查询速度”和“存储效率”之间找到最优解，这也是为什么 B+ 树成为索引的“标准答案”～ 😊











### 36.mysql三大日志

MySQL 的日志系统是其稳定运行和高效优化的核心工具，其中 **二进制日志（Binlog）、错误日志（Error Log）、慢查询日志（Slow Query Log）** 是最关键的三大日志。它们分别服务于不同的场景，共同保障数据库的安全性、可维护性和性能。



#### **一、二进制日志（Binary Log，简称 Binlog）**

**核心作用**：记录所有 **修改数据的 SQL 操作**（如 `INSERT`、`UPDATE`、`DELETE`），用于 **数据恢复** 和 **主从复制**。

##### **1. 记录内容**

Binlog 记录的是 **逻辑操作**（如 `UPDATE user SET age=20 WHERE id=1`），而非物理数据页的变化。具体内容包括：

- 操作类型（`INSERT`/`UPDATE`/`DELETE`）；
- 涉及的表和字段；
- 操作前后的数据变化（部分格式下）；
- 事务信息（如 `BEGIN`、`COMMIT`）。

##### **2. 日志格式**

MySQL 支持三种 Binlog 格式，影响记录的详细程度和恢复能力：

| **格式**      | **全称**            | **特点**                                                     | **适用场景**                     |
| :------------ | :------------------ | :----------------------------------------------------------- | :------------------------------- |
| **STATEMENT** | 基于 SQL 语句的日志 | 记录原始 SQL 语句（如 `UPDATE user SET age=20 WHERE id=1`）。 | 简单场景（如单库单表）。         |
| **ROW**       | 基于行的日志        | 记录每行数据的变化（如“行 100 的 age 从 18 改为 20”）。      | 复杂场景（如多表关联、触发器）。 |
| **MIXED**     | 混合格式（默认）    | 结合 STATEMENT 和 ROW，自动选择更安全的格式（如对触发器用 ROW，普通操作用 STATEMENT）。 | 通用场景（推荐）。               |

##### **3. 核心价值**

- **数据恢复**：误删数据时，可通过 `mysqlbinlog`工具解析 Binlog，恢复到指定时间点的状态；
- **主从复制**：主库将 Binlog 发送给从库，从库重放日志实现数据同步（MySQL 主从架构的核心）；
- **审计**：记录所有数据修改操作，满足合规性要求（如金融行业的操作审计）。





#### **二、错误日志（Error Log）**

**核心作用**：记录 MySQL 运行过程中的 **错误、警告和提示信息**，是排查数据库问题的“第一手资料”。

##### **1. 记录内容**

Error Log 记录的内容包括但不限于：

- 数据库启动/关闭时的错误（如端口被占用、数据目录权限不足）；
- 连接错误（如客户端连接超时、认证失败）；
- SQL 语法错误（如 `SELECT * FOM user`拼写错误）；
- 事务回滚信息（如死锁导致的事务回滚）；
- 内存/磁盘不足的警告（如 `Out of memory`）。

##### **2. 核心价值**

- **快速排错**：遇到数据库无法启动、连接失败等问题时，优先查看 Error Log 定位原因；
- **监控预警**：通过日志中的错误频率（如频繁死锁），发现系统潜在问题（如索引缺失、事务过长）；
- **合规审计**：记录关键操作的失败原因（如删除表失败），满足安全审计要求。





#### **三、慢查询日志（Slow Query Log）**

**核心作用**：记录 **执行时间超过阈值** 的 SQL 语句，用于 **优化查询性能**。

##### **1. 触发条件**

慢查询日志默认关闭，需手动配置阈值（`long_query_time`），仅记录执行时间超过该阈值的 SQL。例如：

```
[mysqld]
slow_query_log=1                  # 启用慢查询日志（1=启用，0=关闭）
long_query_time=2                 # 阈值：执行时间超过 2 秒的 SQL（单位：秒）
log_queries_not_using_indexes=1   # 记录未使用索引的查询（可选）
```

##### **2. 记录内容**

慢查询日志记录的信息包括：

- SQL 语句本身；
- 执行时间（`Query_time`）；
- 锁等待时间（`Lock_time`）；
- 扫描的行数（`Rows_examined`）；
- 返回的行数（`Rows_sent`）；
- 数据库用户（`User`）和主机（`Host`）。

##### **3. 核心价值**

- **定位慢查询**：找到执行时间过长的 SQL（如全表扫描的 `SELECT *`）；

- **优化索引**：通过 `Rows_examined`和 `Rows_sent`的差距（如扫描 10 万行仅返回 10 行），判断是否需要添加索引；

- **调整业务逻辑**：发现高频慢查询后，优化业务逻辑（如分页查询改为缓存）。

	

#### **四、三大日志的对比与总结**

| **日志类型**                 | **核心用途**       | **记录内容**                                 | **关键配置参数**                    |
| :--------------------------- | :----------------- | :------------------------------------------- | :---------------------------------- |
| 二进制日志（Binlog）         | 数据恢复、主从复制 | 修改数据的 SQL 操作（逻辑/行级）             | `log-bin`、`binlog-format`          |
| 错误日志（Error Log）        | 排查问题、监控预警 | 启动/连接/语法错误、事务回滚等               | `log-error`、`log-warnings`         |
| 慢查询日志（Slow Query Log） | 优化查询性能       | 执行时间超阈值的 SQL（含执行时间、扫描行数） | `slow_query_log`、`long_query_time` |

#### 五 、总结

- **Binlog**：主从复制和数据恢复的“核心日志”，必须启用；
- **Error Log**：数据库“健康晴雨表”，遇到问题第一时间查看；
- **慢查询日志**：性能优化的“指南针”，定期分析可大幅提升数据库效率。







## 设计模式

### 37.单例模式?双重锁检查?

#### **(1)单例模式：一个类，一个实例**

单例模式（Singleton Pattern）是设计模式中最简单的一种，核心目标是 **确保一个类在整个程序运行期间仅创建一个实例**，并提供一个全局访问点来获取该实例。

单例模式的实现需满足两个条件：

- **私有构造函数**：禁止外部通过 `new`直接创建实例；

- **全局访问点**：提供一个静态方法（如 `getInstance()`）返回唯一实例。

常见实现方式有两种，各有优缺点：

 **1. 饿汉式**

**特点**：类加载时就创建实例（“饿汉”急着吃），线程安全但可能浪费资源。

**代码示例（Java）**：

```java
public class EagerSingleton {
    // 1. 私有构造函数（禁止外部 new）
    private EagerSingleton() {}

    // 2. 类加载时直接创建实例（静态变量）
    private static final EagerSingleton INSTANCE = new EagerSingleton();

    // 3. 全局访问点
    public static EagerSingleton getInstance() {
        return INSTANCE;
    }
}
```



**2.懒汉式**

**特点**：延迟到首次使用时创建实例（“懒汉”不着急吃），节省资源但非线程安全。

**代码示例（Java）**：

```java
public class LazySingleton {
    // 1. 私有构造函数
    private LazySingleton() {}

    // 2. 初始为 null（延迟创建）
    private static LazySingleton INSTANCE;

    // 3. 全局访问点（首次调用时创建）
    public static LazySingleton getInstance() {
        if (INSTANCE == null) { // 第一次检查
            INSTANCE = new LazySingleton(); // 创建实例
        }
        return INSTANCE;
    }
}
```

若两个线程同时调用 `getInstance()`，可能同时通过 `INSTANCE == null`检查，导致创建多个实例（违反单例）。



#### (2)**双重锁检查：线程安全的懒汉式**

为解决懒汉式的线程安全问题，引入 **双重锁检查**。其核心思想是：**先检查实例是否存在，再通过同步锁确保仅一个线程创建实例**。

 **1. 双重锁检查的实现逻辑**

1. **第一次检查（无锁）**：若实例已存在（`INSTANCE != null`），直接返回，避免不必要的锁竞争；
2. **同步锁**：若实例不存在，进入 `synchronized`同步块（锁定类的 Class 对象），确保只有一个线程进入；
3. **第二次检查（加锁后）**：在同步块内再次检查实例是否存在（防止多个线程同时通过第一次检查，导致重复创建）。

 **2. 关键代码示例（Java）**

```java
public class DoubleCheckedSingleton {
    // 1. 私有构造函数
    private DoubleCheckedSingleton() {}

    // 2. 实例变量需用 volatile 修饰（关键！）
    private static volatile DoubleCheckedSingleton INSTANCE;

    // 3. 全局访问点（双重锁检查）
    public static DoubleCheckedSingleton getInstance() {
        // 第一次检查（无锁，快速判断）
        if (INSTANCE == null) { 
            // 加锁（仅允许一个线程进入同步块）
            synchronized (DoubleCheckedSingleton.class) { 
                // 第二次检查（防止多个线程同时通过第一次检查）
                if (INSTANCE == null) { 
                    INSTANCE = new DoubleCheckedSingleton(); // 创建实例
                }
            }
        }
        return INSTANCE;
    }
}
```

 **3. 为什么需要 `volatile`？**

在 Java 中，`INSTANCE = new DoubleCheckedSingleton()`并非原子操作，它分为三步：

1. 分配内存空间；
2. 初始化对象；
3. 将 `INSTANCE`指向内存地址。

若没有 `volatile`，JVM 可能因 **指令重排序**（如先执行步骤 1 和 3，再执行步骤 2），导致其他线程在 `INSTANCE`未完全初始化时就获取到引用（指向一个“半成品”对象），引发错误。

`volatile`的作用是：

- **防止指令重排序**：确保步骤 2（初始化）完成后，才执行步骤 3（赋值引用）；
- **保证可见性**：所有线程都能看到 `INSTANCE`的最新值（避免缓存不一致）。





#### **(3)总结**

- **单例模式**：确保一个类仅一个实例，提供全局访问点；
- **懒汉式**：延迟加载但非线程安全；
- **双重锁检查**：通过“两次检查 + 同步锁 + `volatile`”实现线程安全的延迟加载；
- **关键点**：`volatile`防止指令重排序，同步锁保证原子性，两次检查避免重复创建。





















